# local env & secrets
# (the placeholder template IS tracked; real .env stays ignored)
.env
.env.*
!.env.example
AGENTS.md
# internal/private notes stay untracked; the public guides in docs/*.md are tracked
docs/internal/

# captured logs
logs/
*.log

# python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/

# dependencies
node_modules/

# build / cache
dist/
build/
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/


graphify-out/

# built React SPA — produced by the frontend build / Docker image, not tracked
app/static-react/
frontend/.vite/
frontend/vite.config.js.timestamp-*

# os / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# docker / local database data
data/
postgres-data/
docker-data/
*.db
*.sqlite
*.sqlite3

# local uploads / temp
uploads/
local_uploads/
tmp/
temp/

# secrets / keys
*.pem
*.key
*.crt
service-account*.json
# Generated mongot secret — never commit (created by install.sh / run.sh)
config/pwfile
# Generated replica-set keyfile for optional Mongo auth — never commit
config/keyfile

# Example/demo app committed to its OWN GitHub repo (has its own .git)
example-library-api/
